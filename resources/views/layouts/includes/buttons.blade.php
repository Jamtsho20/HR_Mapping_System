  <button type="submit" id="submitBtn" class="btn btn-primary"><i class="fa fa-upload"></i> {{ $buttonName ?? 'Submit' }}</button>
  <a href="{{ $cancelUrl ?? '#' }}" class="btn btn-danger"><i class="fa fa-undo"></i> {{ $cancelName ?? 'Cancel' }}</a>
